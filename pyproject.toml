[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "gocomet-nova"
version = "0.1.0"
description = "Single-document trade review proof of concept"
requires-python = ">=3.11"
dependencies = []

[tool.setuptools.packages.find]
include = ["nova*"]
